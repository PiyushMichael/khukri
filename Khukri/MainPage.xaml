<Page
    x:Class="Khukri.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Khukri"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    RequestedTheme="Dark"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


    <Grid>
        <StackPanel
            x:Name="contentPanel"
            Margin="120,30,10,10"
        >
            <TextBlock HorizontalAlignment="Left" Text="Hello World" FontSize="36"/>
            <TextBlock Text="What's your name?"/>
            <StackPanel x:Name="inputPanel" Orientation="Horizontal" Margin="0,20,0,20">
                <TextBox x:Name="nameInput" Width="300" HorizontalAlignment="Left"/>
                <Button x:Name="Button" Content="Say &quot;Hello&quot;" Click="Button_Click"/>
                <Button x:Name="SecondButton" Content="try" Click="SecondButton_Click" Margin="20,0,0,0"/>
            </StackPanel>
            <TextBlock x:Name="greetingOutput"/>
            <TextBlock x:Name="dragBox" Margin="0, 10, 0, 10"/>
            <StackPanel
                AllowDrop="True"
                DragOver="Grid_DragOver"
                Drop="Grid_Drop"
                DragLeave="Grid_DragLeave"
                Background="DarkGray"
                Width="300"
                Height="100"
                HorizontalAlignment="Left"
                CornerRadius="20"
            >
                <TextBlock Text="Drop files here..." HorizontalAlignment="Center" Margin="0, 40, 0, 0"/>
            </StackPanel>
            <TextBlock x:Name="outputBox" Margin="0, 10, 0, 10"/>
        </StackPanel>
    </Grid>
</Page>
