<Page
    x:Class="Khukri.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Khukri"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    RequestedTheme="Dark"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


    <Grid>
        <ScrollViewer>
            <StackPanel
                x:Name="contentPanel"
                Margin="120,30,10,10"
            >
                <TextBlock HorizontalAlignment="Left" Text="Henlo :)" FontSize="36"/>
                <TextBlock Text="What's your name?"/>
                <StackPanel x:Name="inputPanel" Orientation="Horizontal" Margin="0,20,0,20">
                    <RichEditBox x:Name="nameInput" Width="300" Height="100" HorizontalAlignment="Left"/>
                    <Button x:Name="Button" Content="Run" Click="Button_Click" Margin="40,0,0,0"/>
                </StackPanel>
                <TextBlock x:Name="dragBox"/>
                <StackPanel Orientation="Horizontal">
                    <StackPanel
                        AllowDrop="True"
                        DragOver="Grid_DragOver"
                        Drop="Grid_Drop"
                        DragLeave="Grid_DragLeave"
                        Background="DarkGray"
                        Width="200"
                        Height="60"
                        CornerRadius="20"
                        HorizontalAlignment="Left"
                    >
                        <TextBlock Text="Drop files here..." HorizontalAlignment="Center" Margin="0, 20, 0, 0"/>
                    </StackPanel>
                    <Button Content="+" Margin="160, 0, 0, 0" Click="Add_Click"/>
                    <Button Content="-" Margin="5, 0, 0, 0" Click="Subtract_Click"/>
                </StackPanel>
                <StackPanel x:Name="textFields" Margin="0, 20, 0, 0"></StackPanel>
                <TextBlock x:Name="outputBox" Margin="0, 10, 0, 10"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
